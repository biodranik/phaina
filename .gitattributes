# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto
*.sh  text eol=lf
# Need to force test files to have LF line endings. Multiline string comparison fails because of line endings on Windows environment.
tests*.php  text eol=lf